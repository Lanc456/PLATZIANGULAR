import { LogicaMatematicasPipe } from './logica-matematicas.pipe';

describe('LogicaMatematicasPipe', () => {
  it('create an instance', () => {
    const pipe = new LogicaMatematicasPipe();
    expect(pipe).toBeTruthy();
  });
});
