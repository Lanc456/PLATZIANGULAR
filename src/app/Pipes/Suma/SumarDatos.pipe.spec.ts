import { SumarDatos } from './Sumar-Datos.pipe';

describe('LogicaMatematicasPipe', () => {
  it('create an instance', () => {
    const pipe = new SumarDatos();
    expect(pipe).toBeTruthy();
  });
});
